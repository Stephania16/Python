# Escribir una funcion evalQuadratic(a,b,c,x), que devuelve el valor de un polinomio cuadrático de este tipo: a*x^2 + b*x + c.
def evalQuadratic(a, b, c, x):
    '''
    a, b, c, x: valores numéricos 
    '''
    return (a*(x**2)) + (b*x) + c